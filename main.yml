---
- name: Zabbix Agent 2
  hosts: all 
  become: yes
  vars: 
    zabbix_version: 6.0
  tasks:
    - name: Install Debian
      include: "tasks/debian.yml"
      when:
        - ansible_os_family == "Debian"